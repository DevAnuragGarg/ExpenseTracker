import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  emptyRootView: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  emptyButton: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: 24,
  },
  chart: {
    flex: 1,
    margin: 20,
    alignSelf: 'center',
  },
});
