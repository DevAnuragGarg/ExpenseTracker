import { View, Text } from 'react-native';
import { AddExpenseScreenProps } from './props.type';

export const AddExpenseScreen: React.FC<AddExpenseScreenProps> = _ => {
  return (
    <View>
      <Text>{`Hey`}</Text>
    </View>
  );
};
